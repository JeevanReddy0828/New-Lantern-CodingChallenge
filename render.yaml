services:
  - type: web
    name: relevant-priors
    runtime: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    envVars:
      - key: LOG_LEVEL
        value: INFO
